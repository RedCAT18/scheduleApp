//action creator

//initial state

INITIAL_STATE = {
  isLoggedIn: false
};

//reducer

function reducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default reducer;
